<%= form_with(model: client_legal_form) do |form| %>
  <% if client_legal_form.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(client_legal_form.errors.count, "error") %> prohibited this client_legal_form from being saved:</h2>

      <ul>
        <% client_legal_form.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :client_id, style: "display: block" %>
    <%= form.select :client_id, Client.all.map { |client| [client.first_name, client.id] }%>
  </div>

  <div>
    <%= form.label :legal_form_id, style: "display: block" %>
    <%= form.select :legal_form_id, LegalForm.all.map { |legal_form| [legal_form.name, legal_form.id] }%>
  </div>

<!--
  <div>
    <%#= form.label :first_login_date, style: "display: block" %>
    <%#= form.datetime_field :first_login_date %>
  </div>

  <div>
    <%#= form.label :most_recent_login, style: "display: block" %>
    <%#= form.datetime_field :most_recent_login %>
  </div>
-->

  <div>
    <%= form.submit %>
  </div>
<% end %>
