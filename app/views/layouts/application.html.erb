<!DOCTYPE html>
<html>
  <head>
    <title>EvansDixonLegalForms</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="container">
      <div class="grid grid-cols-2">
        <div class="bg-blue-950 text-white text-2xl p-4">
          <%= link_to "Evans Dixon", '/', class: "text-white"%>
        </div>
        <div class="bg-blue-950 text-white text-2xl p-4 text-right">
          <%= link_to "View Clients", clients_path, :method => "get", class: 'text-white' %>
          <%= link_to "View Forms", client_legal_forms_path, :method => "get" , class: 'text-white' %>
        </div>
      </div>
      <%= yield %>
    </main>
  </body>
</html>
